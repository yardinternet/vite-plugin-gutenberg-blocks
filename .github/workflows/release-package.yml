name: Package Release

on:
    release:
        types: [created]

jobs:
    publish-gpr:
        runs-on: ubuntu-latest
        permissions:
            packages: write
            contents: read
        steps:
            # Checkout code
            - uses: actions/checkout@v3
              with:
                  ref: main

            # Setup Node.js
            - uses: actions/setup-node@v3
              with:
                  node-version: 22
                  registry-url: https://npm.pkg.github.com/

            # Corepack reads the pnpm version from package.json's "packageManager" field
            - run: corepack enable

            # Install dependencies using pnpm
            - run: pnpm install

            # Build the package
            - run: pnpm run build

            # Publish to GitHub Packages (private org package)
            - run: pnpm publish --access restricted --no-git-checks
              env:
                  NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
